# 火山引擎 DeepSeek API 集成指南

## 概述

本项目已集成火山引擎的 DeepSeek API，用于为用户的城市政策问题提供智能回答。

## API 路由

**路由地址**: `/api/chat`  
**方法**: `POST`  
**请求格式**: JSON

### 请求体示例

```json
{
  "question": "基本农田可以转为建设用地吗？"
}
```

### 响应格式

**成功响应（200）**:
```json
{
  "answer": "根据《中华人民共和国土地管理法》..."
}
```

**错误响应**:
```json
{
  "error": "错误描述信息"
}
```

## 环境变量配置

### 1. 创建本地环境变量文件

在项目根目录创建 `.env.local` 文件：

```bash
touch .env.local
```

### 2. 配置环境变量

编辑 `.env.local` 并添加以下内容：

```
VOLC_ACCESS_KEY=你的API密钥
VOLC_MODEL_ID=你的模型ID
```

### 3. 获取环境变量值

**VOLC_ACCESS_KEY**:
- 访问 [火山引擎控制台](https://console.volcengine.com/)
- 登录账户
- 进入 API 密钥管理
- 创建或复制现有的 API Key

**VOLC_MODEL_ID**:
- 在火山引擎创建一个 DeepSeek 模型实例
- 获取该模型的 ID
- 通常格式为 `ep-xxx...` 或类似的模型标识符

## 功能说明

### 前端集成

当用户在首页提问或点击热门问题时：
1. 搜索框捕获问题文本
2. 前端发送 POST 请求到 `/api/chat`
3. API 调用火山引擎 DeepSeek API
4. 返回的答案显示在结果页面
5. 用户可以进行反馈或返回首页

### 后端处理流程

1. **验证输入**: 确保问题文本不为空且为字符串类型
2. **检查环境变量**: 验证 API Key 和 Model ID 已配置
3. **调用 API**: 向火山引擎发送请求
4. **错误处理**: 处理网络错误、API 错误等
5. **提取答案**: 从 API 响应中提取回答内容
6. **返回结果**: 以统一格式返回给前端

## 加载状态

当用户提问时，结果页面会显示：
- 一个旋转的加载动画
- "正在查询政策数据库..."的提示文本

API 返回答案后，加载动画消失，政策卡片和反馈按钮显示。

## 错误处理

系统会捕获并处理以下错误：

| 错误类型 | 处理方式 |
|---------|--------|
| 缺少环境变量 | 返回"服务器配置不完整"错误 |
| 无效输入 | 返回"问题文本不能为空"错误 |
| API 请求失败 | 返回具体的 HTTP 状态和错误信息 |
| JSON 解析错误 | 返回"处理请求时出错"错误 |

## 日志

所有 API 调用都会被记录，可在开发服务器的控制台查看：

```
[v0] 成功调用 API，问题: 基本农田可以转为建设用地吗...
[v0] API 路由错误: ...
[v0] 缺少环境变量：VOLC_ACCESS_KEY 或 VOLC_MODEL_ID
```

## 部署到 Vercel

1. 在 Vercel 项目设置中添加环境变量:
   - `VOLC_ACCESS_KEY`
   - `VOLC_MODEL_ID`

2. 重新部署应用

3. API 路由会自动在生产环境中可用

## 测试

### 使用 curl 测试

```bash
curl -X POST http://localhost:3000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"question":"基本农田可以转为建设用地吗？"}'
```

### 使用前端页面测试

1. 启动开发服务器: `pnpm dev`
2. 在首页输入问题
3. 点击"提问"按钮
4. 等待结果页面显示答案

## 常见问题

**Q: 显示"服务器配置不完整"**  
A: 检查是否正确创建了 `.env.local` 文件并配置了 `VOLC_ACCESS_KEY` 和 `VOLC_MODEL_ID`

**Q: API 返回 401 Unauthorized**  
A: API Key 无效或已过期，请在火山引擎控制台重新检查和更新

**Q: 响应时间很长**  
A: DeepSeek 模型可能需要一些时间进行处理，这是正常的。可以在 `max_tokens` 参数中调整返回内容的最大长度

**Q: 答案内容不准确**  
A: 可以调整系统提示词或在结果页使用反馈功能报告问题
